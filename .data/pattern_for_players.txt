CREATE TABLE IF NOT EXISTS players (
    name VARCHAR(10) PRIMARY KEY NOT NULL,
    money INTEGER DEFAULT 0 NOT NULL,
    tank_speed TINYINT DEFAULT 2 NOT NULL,
    bullet_speed TINYINT DEFAULT 8 NOT NULL,
    reload_ticks TINYINT DEFAULT 100 NOT NULL
);
