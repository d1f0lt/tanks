CREATE TABLE IF NOT EXISTS skills (
    name VARCHAR(10) PRIMARY KEY NOT NULL,
    tank_speed INTEGER DEFAULT 2 NOT NULL,
    bullet_speed INTEGER DEFAULT 8 NOT NULL,
    reload_ticks INTEGER DEFAULT 100 NOT NULL
)